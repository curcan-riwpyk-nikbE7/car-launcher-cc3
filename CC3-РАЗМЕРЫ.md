# TEYES CC3 — замеры интерфейса

Всё замерено **пипеткой по скриншоту 1280×720** (родное разрешение ГУ,
без масштабирования) и по официальной галерее «UI display collection».

Плотность экрана: 1 dp = 1 px, поэтому пиксели = dp напрямую.

---

## Боковая панель

| Элемент | Размер |
|---|---|
| **Ширина панели** | **130 px** (10.2% ширины экрана) |
| Орб-микрофон (диаметр) | **79 px** |
| Орб: центр по X | 61 px |
| Орб: центр по Y | 255 px |
| Серые иконки (шестерёнка, куб) | **44 px** |
| Шаг между иконками | **129 px** |
| Иконка 1 (настройки), центр Y | 379 px |
| Иконка 2 (куб), центр Y | 508 px |
| Часы: высота цифр | 19 px |
| Точки сети + «4G» | y 126…160 |
| Синяя кнопка навигации снизу | высота **127 px**, от y=593 до низа |

Порядок сверху вниз: **часы → точки сети/4G → орб → шестерёнка →
куб → (растяжка) → синяя кнопка-стрелка в углу**.

Синяя кнопка внизу имеет **скошенный верхний-левый угол** (диагональный срез).

## Цвета (эталон)

| Что | HEX |
|---|---|
| Фон панели | `#14193F` |
| Фон экрана | `#141126` |
| Фон карточки | `#1D2242` |
| Синяя кнопка навигации | `#4E8ECB` |

## Раскладка главного экрана

Верхний ряд: **медиа-карточка** (слева, уже) + **карточка авто со
спидометром** (справа, шире).
Нижний ряд: **«Приложения»** (5 иконок в ряд, подписи под ними) +
**«Радио»** (частота, название, кнопки, эквалайзер-визуализация).

Статусная строка — **только над правой частью**, не над панелью:
дата, погода, микрофон, Bluetooth, Wi-Fi, громкость.

---

## Остальные экраны (из официальной галереи)

### Приложения
Сетка **3 колонки × 4 ряда**. Плитка горизонтальная: **иконка слева
в квадрате со скруглением, подпись справа**. Не привычная сетка
«иконка сверху, подпись снизу» — именно горизонтальные плитки.

### Плеер
Обложка на **всю правую половину** экрана, слева — название, исполнитель,
прогресс с временем, крупные кнопки. Внизу: повтор, лайк, эквалайзер.
Отдельный экран плейлиста: вкладки «Все / Избранное / Внутренние»
слева, список справа, мини-плеер снизу.

### Радио
Крупная частота **по центру сверху** (102.40 MHz), стрелки влево/вправо
по бокам, под ней — **шкала-линейка частот**. Ниже ряд **логотипов
станций** плитками. Внизу панель: Band, поиск, RDS, PTY, TA, AF, настройки.

### Bluetooth-музыка
**Телефон в рамке по центру** с неоновым свечением по контуру,
внутри — название трека, прогресс, кнопки. Это подтверждает наш
`PhoneMediaCard` — он сделан правильно.

### Телефон
Клавиатура: слева вертикальная панель вкладок (набор, контакты, поиск,
журнал, настройки), справа — цифры 3×4 крупными плитками.
Входящий звонок: аватар, номер, таймер, красная кнопка сброса.

### Настройки
Сверху **горизонтальные вкладки-пилюли**: WLAN, Device, Common,
Factory, Personal, System. Активная подсвечена.
Содержимое — **два столбца** переключателей и пунктов со стрелками.

### Шторка (Control Panel)
Опускается сверху, **тёмное затемнение с размытием**. Слева время
крупно + дата + погода, справа шестерёнка.
Ряд из **6 круглых кнопок**: WiFi, Mobile data, Switch theme,
Display time, Off screen, Restart. Подписи под кружками.
Снизу — **широкий ползунок яркости** с иконкой солнца слева,
скруглённый полностью. Внизу по центру — полоска-«ручка».

### Split screen
Экран делится **пополам вертикально**: слева навигация, справа плеер.

---

## Что это значит для нашего лаунчера

Наша панель сейчас **75 px** — почти вдвое уже штатных 130 px.
Из-за этого орб и иконки мельче, по ним труднее попадать на ходу.

Приводим к CC3: панель 130, орб 79, иконки 44, шаг 129.
